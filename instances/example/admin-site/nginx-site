server {
  listen 80;
  listen [::]:80;
  server_name admin-test.intake24.co.uk;

  client_max_body_size 10m;
  
  location / {
    proxy_pass http://localhost:8001/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
