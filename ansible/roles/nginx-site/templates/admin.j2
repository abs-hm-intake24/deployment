server {
  listen 80;
  listen [::]:80;
  server_name {{ admin_site.host_name }};

  location / {
    proxy_pass http://localhost:{{admin_site.local_port}}/;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
